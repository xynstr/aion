# AION Dependencies
# =================

# Core
openai>=1.0.0
tomli>=2.0.0;python_version<"3.11"  # tomllib stdlib backport for Python 3.10
httpx>=0.24.0
beautifulsoup4>=4.12.0
ddgs>=9.0.0                # DuckDuckGo search (ersetzt html-scraping das geblockt wird)

# Rich CLI Styling (optional aber recommended)
rich>=13.0.0

# Web UI
fastapi>=0.104.0
uvicorn>=0.24.0

# Environment variables
python-dotenv>=1.0.0

# TTS (optional — engine=edge empfohlen, engine=sapi5 als Fallback)
edge-tts>=6.1.0

# Speech-to-Text (optional — audio_transcriber + audio_pipeline plugins)
# faster-whisper>=1.0.0    # Offline STT — requires C++ runtime on Windows, install manually if needed:
#                          #   pip install faster-whisper

# Credentials Vault (credentials plugin — AES-Verschlüsselung)
cryptography>=42.0.0

# File processing (Web UI uploads)
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0

# Browser automation (playwright_browser plugin)
# After install: playwright install chromium
playwright>=1.40.0

# AI Provider SDKs (optional — je nach gewähltem Provider)
google-genai>=0.8.0        # Google Gemini (gemini_provider plugin)

# Messaging channels (optional — je nach gewünschten Kanälen)
discord.py>=2.3.0          # Discord-Bot (discord_bot plugin)
slack-bolt>=1.18.0         # Slack-Bot (slack_bot plugin)

# MCP (Model Context Protocol) client
mcp>=1.0.0                 # MCP server connections (mcp_client plugin)

# Desktop automation (desktop plugin — screenshot, click, type, hotkeys)
pyautogui>=0.9.54          # Cross-platform desktop control
Pillow>=10.0.0             # Screenshot support for pyautogui
